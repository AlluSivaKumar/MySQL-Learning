CREATE DATABASE UNIVERSITY;

USE UNIVERSITY;

CREATE TABLE STUDENT (
NAME VARCHAR(20),
AGE INT PRIMARY KEY,
MARKS FLOAT);

#PRAGMA TABLE_INFO(STUDENT); WORKS IN MY SQL

DESCRIBE STUDENT;

INSERT INTO STUDENT(NAME , AGE, MARKS)
VALUES
('SIVA',22,95.5),
('RAKESH',23,96.8),
('MURALI',21,92.5);

SELECT *
FROM STUDENT;

SELECT NAME , AGE
FROM STUDENT;

SELECT * 
FROM STUDENT
WHERE AGE > 22;

SET SQL_SAFE_UPDATES = 0;

UPDATE STUDENT
SET MARKS = 96.1
WHERE NAME = 'SIVA';

DELETE FROM
STUDENT
WHERE NAME = 'SIVA';

DROP TABLE STUDENT;

ALTER TABLE STUDENT
ADD COLUMN ROLLNO INT;

ALTER TABLE STUDENT
RENAME COLUMN ROLLNO TO ROLLNUM;

ALTER TABLE STUDENT
DROP COLUMN ROLLNUM;

SELECT * 
FROM STUDENT
WHERE NAME = 'SIVA';

SELECT * 
FROM STUDENT
WHERE NAME <> 'SIVA';

SELECT * 
FROM STUDENT
WHERE NAME LIKE 'S%';

SELECT NAME 
FROM STUDENT
WHERE NAME LIKE '%R%';

SELECT *
FROM STUDENT
WHERE NAME LIKE '______';

SELECT * 
FROM STUDENT
WHERE NAME LIKE '______' 
AND 
NAME LIKE 'R%';

SELECT * 
FROM STUDENT
WHERE (NAME LIKE '______' 
OR
NAME LIKE 'R%');

SELECT * 
FROM STUDENT
WHERE NAME NOT LIKE '______';

SELECT * 
FROM STUDENT
WHERE NOT NAME LIKE '______';

SELECT * 
FROM STUDENT
WHERE NAME IN ('SIVA', 'RAKESH');

SELECT *
FROM STUDENT
WHERE AGE BETWEEN 22 AND 23; 

SELECT * 
FROM STUDENT
ORDER BY NAME;

SELECT * 
FROM STUDENT
ORDER BY NAME ASC;

SELECT * 
FROM STUDENT
ORDER BY NAME DESC;

SELECT *
FROM STUDENT
WHERE NAME LIKE '______'
ORDER BY NAME ASC;

SELECT *
FROM STUDENT
WHERE NAME LIKE '______'
ORDER BY NAME DESC;

SELECT DISTINCT NAME
FROM STUDENT;

SELECT * 
FROM STUDENT
ORDER BY NAME ASC
LIMIT 2;

SELECT * 
FROM STUDENT
ORDER BY NAME DESC
LIMIT 2;

SELECT *
FROM STUDENT
ORDER BY NAME ASC
LIMIT 2 
OFFSET 0;

SELECT AVG(AGE)
FROM STUDENT;

SELECT SUM(MARKS)
FROM STUDENT
WHERE NAME LIKE '______';

SELECT 
MIN(MARKS),
MAX(MARKS)
FROM STUDENT;	

SELECT COUNT(*)
FROM STUDENT;
